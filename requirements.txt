# Pinned versions used for the journal experiments (Python 3.12.11,
# macOS arm64). Statistical conclusions are platform-independent;
# bit-exact trajectory reproduction is guaranteed only on the same
# platform/BLAS (see README).
stable-baselines3==2.7.0
gymnasium==1.2.0
numpy==2.2.6
scipy==1.16.2
scikit-learn==1.7.2
matplotlib==3.10.6
torch==2.8.0
pytest>=7.4
# Optional extras (interactive progress bar + paper figure rendering).
# Not required to run the simulation or tests; pinned here for full
# reproduction of the authors' environment.
tqdm==4.67.1
rich==13.7.1
Pillow==11.3.0
